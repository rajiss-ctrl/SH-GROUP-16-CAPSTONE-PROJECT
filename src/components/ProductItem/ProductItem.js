import React from 'react';
import './ProductItem.css'

// This is the Card component for the {Product Item} on the Product Page

export default function ProductItem(Prop, img, price, button) {
  return (
    <div>
        
    </div>
  );
}
